# Block Set 语义说明

## 1. 程序模型

- 一个 `.bs` 文件整体视为一个隐式顶层 `block`。
- 名为 `main` 的 `block` 是入口点。
- `compiler "…";` 是文件级编译器指令（如 `compiler "import <studio>";`），语义由编译器实现决定。

---

## 2. Block 与作用域

### 2.1 Block 声明

```
block 名称 [参数列表?] { 语句* };
```

- `block` 声明的变量是**全局可见**的。
- 参数列表 `[x: Type, y]` 可选（0 或 1 次），参数可以带类型标注，也可以不带。

### 2.2 变量暴露

变量只有通过以下方式才会暴露给外部：

| 方式 | 语法 | 说明 |
|------|------|------|
| `var` 声明 | `var \| x: Int, y;` | 显式暴露 |
| 方括号参数 | `block Foo[x: Int] { }` | 参数自动暴露 |

既没有 `var` 也没有作为 block 参数的变量，是**匿名的**。匿名变量在外部等价于 `block 匿名 { 匿名 <- null; }`，外部路径访问得到 `null`。

### 2.3 遮蔽规则

> 上层暴露的变量会遮蔽内部，内部未暴露的变量仅限于该 `block` 。 `@` 在 `Block` 内仍然生效。


暴露与否决定命名冲突时的优先级。

### 2.4 命名冲突规则

一个 block 不能与其**父级、同级、子级** block 重名。

---

## 3. 路径系统

### 3.1 根引用 `$`

`$` 始终指代根 block（文件顶层）。

### 3.2 子级导航 `.`

`a.b`：在 `a` 的子 block 中查找 `b`。
找不到则尝试父 block。
仍找不到则视为空子 block（取值 `null`）。

### 3.3 同级导航 `..`

`a..b`：在 `a` 的同级 block 中查找 `b`。
找不到则尝试父 block。
仍找不到则视为空间级 block（取值 `null`）。

### 3.4 隐含路径

在 block 体内部，变量引用的隐含前缀就是该 block 自身。block 声明的变量名在体内指向它本身。

---

## 4. `@` 语法糖

```
路径 { … };
```

体中出现的 `@` 都指代该路径。等价于 `PATH_STMT`（`路径 { 体 };`），编译期展开为普通路径访问。

---

## 5. 类型系统

### 5.1 类型即 Block 结构

不存在独立的类型定义语法。**每个 block 的结构本身就是一个类型**。

```
block Point[x: Int, y: Int] { };
block Vector[x: Int, y: Int] {
    block add[q: Point]: Point {
        add.x <- x + q.x;
        add.y <- y + q.y;
    };
};
```

冒号 `:` 在编译前为冒号前的 block 赋予冒号后 block 的结构，并决定该 block **"作为值"时包含哪些字段**。
相当于编译前进行不可变的 `#` 拷贝。

基本类型（`Int`、`String`、`Bool` 等）由系统提供。

### 5.2 `<-` 与 `#` 的语义

| 操作 | 语义 |
|------|------|
| `目标 <- 源` | **类型限定的赋值**：目标类型（通过 `var` 和 `[]` 暴露的字段集合）决定从源提取哪些字段。源的额外字段被丢弃，缺失字段则报错。 |
| `目标 # 源` | **全量拷贝**：无视类型声明，源的所有字段全部拷贝到目标，包括体。 |

- 只要未声明，变量就允许改变类型，但**没有隐式类型转换**。类型不兼容（`<-` 时目标需要的字段源没有）是编译期错误。

---

## 6. 语句一览

| 语句 | 语法 | 语义 |
|------|------|------|
| `BLOCK_STMT` | `block 名 [参数] 体;` | 声明 block |
| `VAR_STMT` | `var \| x: 类型, …;` | 暴露变量 |
| `ASSIGN_STMT` | `路径 <- 值;` | 类型限定的赋值（见 §5.2） |
| `COPY_STMT` | `路径 # 值;` | 全量拷贝（见 §5.2） |
| `CALL_STMT` | `call 路径;` | 调用 block 体并丢弃返回值，**总是可用** |
| `CHAIN_STMT` | `路径;` | 裸路径调用，**仅当目标 block 没有 `[]` 参数声明时可用**，等价于无参调用 |
| `PATH_STMT` | `路径 { 体 };` | `@` 语法糖的展开形式 |
| `BACK_STMT` | `back;` | 退出当前 block |
| `IF_STMT` | `if 值 体 elseif … else …;` | 条件分支，条件必须是 `Bool` |
| `LOOP_STMT` | `loop 标签? \| 体;` | 循环，`\|` 是纯分隔符，无特殊语义 |
| `BREAK_STMT` | `break 标签\|数字?;` | 跳出循环 |
| `CONTINUE_STMT` | `continue 标签\|数字?;` | 继续下一次循环 |

### 6.1 返回值

`back;` 退出当前 block。block 的返回值就是退出后 block 自身的值——在体内对 block 自身的赋值，最终作为 block 的值暴露给调用者。

### 6.2 `call` vs 裸 `CHAIN`

- `call a.b.c;` 总是合法。
- 裸 `a.b.c;` 仅在 `c` 没有 `[]` 参数声明时合法。`c` 有参数则非法。

### 6.3 `break` / `continue` 数字

- 带数字：跳出 / 继续 N 层循环。
- 超出实际嵌套层数：**未定义行为**，编译器可以报错。
- 标签不与 block 共享命名空间。

---

## 7. 表达式（VALUE）

优先级从低到高：

> **OR → AND → 比较 → +/- → ×÷\* / → NOT / - → 原子**

| 层级 | 运算符 | 结合性 |
|------|--------|--------|
| 逻辑或 | `OR` | 左 |
| 逻辑与 | `AND` | 左 |
| 比较 | `>=?` `=?` `<=?` `>?` `<?` `!=?` | 最多一次 |
| 加减 | `+` `-` | 左 |
| 乘除 | `×` `÷` `*` `/` | 左 |
| 一元 | `NOT` `-` | 前缀 |
| 原子 | 数字、字符串、`CHAIN`、`( VALUE )` | — |

### 7.1 布尔类型

- `true` 为真，`false` 为假。
- `if` / `loop` 条件**必须**是 `Bool`，不接受非布尔值，无隐式转换。
- 比较运算符返回 `Bool`。

### 7.2 括号

`( VALUE )` 是表达式括号，内部是 VALUE 而非语句。这区别于出现在 CHAIN 路径中的 `PAREN_BLOCK`（内部是语句，用于参数传递）。

---

## 8. 控制流

### 8.1 `if`

```
if 条件 体
elseif 条件 体
...
else 体
;
```

条件必须是 `Bool`。`elseif` 可多次出现，`else` 可选。终止符为 `;` 或 `end`。

### 8.2 `loop`

```
loop 标签? | 体;
```

- 标签可选，不与 block 共享命名空间。
- `|` 是纯分隔符。

---

## 9. 杂项

### 9.1 `END`

`;` 和 `end` 等价。

### 9.2 注释

- 行注释：`// …` 到行尾
- 块注释：`/* … */`

---

### 9.3 原生类型

`String` 是字符串类型，不预设字符长度。
`String[]` 是携带初始值的字符串类型。
携带字符串类型的变量，通过 `[n]` 可以查询第 `n-1` 个字符串，索引从 `0` 开始。
`Int` 是整数类型，不预设位数。
`Int[]` 是携带初始值的整数类型。
`Bool` 是布尔值类型，不预设实现。
`Bool[]` 是携带初始值的布尔值类型。
`List` 是列表类型，不预设实现。
`List[]` 是指定类型的列表类型，不预设实现。
携带列表类型的变量，通过 `[n]` 可以返回第 `n-1` 个项，索引从 `0` 开始。
`Map` 是字典，不预设实现。
`Number` 是任意的浮点数，不预设实现。
`Number[].NS[]` 是指定自己基于的具体进制与初始值。
