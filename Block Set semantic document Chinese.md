# 语义

## 理念

 `Block Set` 是为组织 LLM 上下文、处理结构化数据（尤其是 `JSON` ）而设计的。它的 `block` 同时是数据结构、`schema` 约束、和转换规则——三者不再需要不同的语言和文件。你可以用它定义 `LLM` 的工具格式、构建多轮对话上下文、约束输出结构、以及在这些结构上执行条件逻辑——全在一个文件里。

 `block` 是 `Block Set` 语言提供的抽象。

 `block` 携带数据和程序，并且 `block` 也是命名空间。

通过 `block` 的各种用法，可以组合出绝大多数范式。

## 链式调用

 `.` 和 `..` 是两个不同的符号，语义也不同。
具体而言

>  `.` 访问的是 `block` 的子 `block` 。
>  `..` 访问的是 `block` 的同级 `block` 。
> 如果点号后面的，既不是子变量，也不是同级变量，那么它们都会检查是不是父 `block` 。
> 如果不是父 `block` ，那么
>  `.` 会认为，这是一个值为 `null` ，体为 `{}` 的子 `block` 。
>  `..` 会认为，这是一个值为 `null` ，体为 `{}` 的同级 `block` 。

并且，链式调用的语义是，点号前的 `block` 返回了一个 `block` ，点号再访问这个 `block` 。

## 类型，匿名与变量的作用域

在 `Block Set` 中，我们有 `var` 语句，以及方括号声明。实际上 `block` 语句也算声明。

被声明的 `block` 就是全局的，否则就是匿名的。

 `var` 声明的名字是 `block` 结构的一部分，外部可以通过 `.` 访问。直接赋值的名字只在 `block` 内部存在，用完就消失。

匿名的 `block` 不能跨越层级被访问，而上级声明的 `block` 会自动覆盖到下级，除非在下级也声明——内部覆盖外部。

在 `block` 语句里，表达式中的非链的名字，默认是在该 `block` 语句正在声明的 `block` 下查找。

 `block` 的值的类型默认是不固定的，除非声明了。

 `Block Set` 没有隐式转换，并且布尔值是独立的类型。

语言标准提供如下类型：

> `String` 是字符串类型，不预设字符长度。
> `String[]` 是携带初始值的字符串类型。
> 携带字符串类型的变量，都自带一个子 `block` `Char`，通过 `.Char[n]` 可以访问第 `n-1` 个字符，索引从 `0` 开始。
> `Int` 是整数类型，不预设位数。
> `Int[]` 是携带初始值的整数类型。
> `Bool` 是布尔值类型，不预设实现。
> `Bool[]` 是携带初始值的布尔值类型。
> `List` 是列表类型，不预设实现。
> `List[]` 是指定类型的列表类型，不预设实现。
> 携带列表类型的变量，都自带一个子 `block` `Item`，通过 `.Item[n]` 可以访问第 `n-1` 项，索引从 `0` 开始。
> `Map` 是字典，不预设实现。
> `Map[]` 是指定类型的字典。
> 携带字典类型的变量，都自带一个子 `block` `Entry`，通过 `.Entry[str]` 可以访问键为 `str` 的项。
> `Number` 是任意的浮点数，不预设实现。
> `Number[].NS[]` 是指定自己基于的具体进制与初始值。

并且，类型在 `Block Set` 里的本质，是在编译期，将冒号后的 `block` `#` 到前面的 `block` 。
类型决定了 `<-` 的语义，也就是什么是值，要传递什么。并且提供检查。

---
 `a <- b;` 是指，`b` 被认为是值的结构被赋给 `a` 。
如果其中一个声明了类型，就按照声明了类型的那一方进行“过滤”。
如果两者都声明了类型，会检查类型是否匹配。
如果两者都没有声明类型，就只会传递 `block` 本身的基本值，也就是不依赖结构的值，而不会传递结构。
---
 `a # b;` 是指， `b` 的类型、子 `block` 以及其本身等，都赋给 `a` 。
包括体在内，此时执行 `a` 和执行 `b` 是同一个程序。
但是，如果体是被 `block` 语句声明了，则不变。
---
 `a = b;` 是指，将 `b` 的引用传递给 `a` 。
此后 `a` 和 `b` 等价，如果 `a` 用 `block` 语句声明了体，则体不变。
如果要解引用，可以通过 `a = ;` 或 `a = a;` 解引用。
前者的 `a` 此时是 `null` 值空体的 `block` ——如果通过 `block` 语句声明了体，则体不变。
后者的 `a` 此时保存着解引用前的快照。
---

在 `Block Set` 中， `$` 表示根目录本身，而 `@` 表示所在的命名空间。

在 `block` 语句中，`@` 表示正在被声明的 `block` 自身。

在 `path` 语句中，表示 `path` 后的那个 `block` 。并且， `path` 语句中的那个 `block` 是默认在顶层开始找的，而非当前 `block` 。
 `path` 语句只改变 `@` 的含义。
可以把 `path` 和 `@` 当成语法糖。

## 组织程序

 `loop` 死循环是该语言唯一的循环，配合 `break` 和 `continue` 使用。
 `break` 的语义是跳转到循环结尾， `continue` 的语义是跳转到循环开始。
 `loop` 的标签仅在 `loop` 体内可见，且仅对 `break` 和 `continue` 有含义。因此不会和 `block` 冲突。
 `break` 和 `continue` 可以携带数字，含义是跳出几层。

 `if` 条件分支判断是 `Block Set` 的分支语句。与 `if` 配套的还有 `elseif` 和 `else` 。
一旦前面的 `if` 或 `elseif` 被触发，就不再尝试后面的分支，否则继续尝试，直到有一个分支可以执行。
如果所有带条件的分支都失败了，则执行 `else` 分支所携带的语句。

 `back` 语句指，退出当前 `block` ，返回被调用处，并将 `block` 自己作为整体返回。

一个示例

```Block Set
// 判断质数

block is_prime [n: Int]: Bool | {

    if n <=? 1 | {
        is_prime <- false;
        back;
    };

    i <- 2;

    loop prime_loop | {
        if i × i >? n | {
            break prime_loop;
        };
        if n % i =? 0 | {
            is_prime <- false;
            back;
        };
        i <- i + 1;
    };

    is_prime <- true;
    back;
};
```

## 算符

语言内置的算符有
 `+`  `-`
 `*`  `/`  `×`  `÷`
 `AND`  `OR`  `NOT` 
 `>?`  `=?`  `<?`  `>=?`  `<=?`  `!=?`

| 层级 | 运算符 | 结合性 |
|------|--------|--------|
| 1（最低） | `OR` | 左 |
| 2 | `AND` | 左 |
| 3 | `>=?` `=?` `<=?` `>?` `<?` `!=?` | 不链式（只允许一个） |
| 4 | `+` `-` | 左 |
| 5 | `×` `÷` `*` `/` | 左 |
| 6 | `NOT` `-`（前缀） | 右（前缀天然右结合） |
| 7（最高） | `NUMBER` `STRING` `CHAIN` `( )` | — |

## compiler语句

 `compiler` 语句是编译期指令，语言不保证实现方式。
